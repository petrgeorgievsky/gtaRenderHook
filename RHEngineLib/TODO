1) Reduce WinAPI dependency
2) Make CMake build work
3) Add linux test app wrapper
4) Comment out uncommented code
5) Add test list that could verify that everything works correctly
6) Move settings utilities here(without AntTweakBar support for now)
7) Add memory allocation utilities like pools and aligned memory allocation
8) Write some code and documentation standart